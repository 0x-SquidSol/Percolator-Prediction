{
  "name": "@percolatorct/prediction-workspace",
  "version": "0.0.1",
  "private": true,
  "description": "Workspace root for Percolator prediction markets — match-pred matcher (rust), sdk-pred SDK staging (ts), and integration tests (ts)",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/0x-SquidSol/Percolator-Prediction.git"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "typecheck": "pnpm -r run typecheck",
    "test": "pnpm -r run test",
    "build": "pnpm -r run build"
  }
}
